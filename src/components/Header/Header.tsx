import React, { FC } from 'react';
import Category from '../Category/Category';

const Header: FC = () => {
    return (
        <header>
            <Category />
        </header>
    );
};

export default Header;